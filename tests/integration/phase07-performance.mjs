import assert from"node:assert/strict";import{performance}from"node:perf_hooks";
const app=process.env.APP_URL;assert(app);const routes={playerProfile:"/players/player-68ea715b81d4e086",matchHistory:"/players/player-68ea715b81d4e086",globalLeaderboard:"/leaderboards",seasonLeaderboard:"/seasons/draftgg-season-one",seasonPage:"/seasons/draftgg-season-one",organizationPage:"/organizations/persistent-phoenix",search:"/search?q=Scale"},results={};
for(const[name,path]of Object.entries(routes)){const times=[];for(let i=0;i<21;i++){const started=performance.now(),response=await fetch(app+path);assert.equal(response.status,200,path);await response.text();if(i)times.push(performance.now()-started)}times.sort((a,b)=>a-b);results[name]={median:Number(times[Math.floor(times.length/2)].toFixed(2)),p95:Number(times[Math.floor(times.length*.95)].toFixed(2)),worst:Number(times.at(-1).toFixed(2))};const target=name.includes("Leaderboard")?250:200;assert(results[name].median<target,`${name} median exceeded ${target}ms`)}console.log(JSON.stringify({result:"PASS",runsPerRoute:20,results}));
