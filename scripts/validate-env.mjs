const production=process.env.VERCEL_ENV==="production"||process.env.NODE_ENV==="production",required=["NEXT_PUBLIC_SITE_URL","NEXT_PUBLIC_SUPABASE_URL","NEXT_PUBLIC_SUPABASE_ANON_KEY","SUPABASE_SERVICE_ROLE_KEY","DOTA_SYNC_SECRET","READINESS_SECRET","RATE_LIMIT_SALT"],missing=required.filter(x=>!process.env[x]),invalid=[];let site;for(const name of ["NEXT_PUBLIC_SITE_URL","NEXT_PUBLIC_SUPABASE_URL"]){try{const url=new URL(process.env[name]);if(name==="NEXT_PUBLIC_SITE_URL")site=url;if(production&&url.protocol!=="https:")invalid.push(`${name}_HTTPS_REQUIRED`);if(production&&["localhost","127.0.0.1"].includes(url.hostname))invalid.push(`${name}_LOCALHOST_FORBIDDEN`);}catch{invalid.push(`${name}_INVALID_URL`);}}if(production&&site&&/staging|preview|localhost/i.test(site.hostname))invalid.push("PRODUCTION_SITE_ENVIRONMENT_MISMATCH");if(process.env.SUPABASE_AUTH_CALLBACK_URL){try{const callback=new URL(process.env.SUPABASE_AUTH_CALLBACK_URL);if(!site||callback.origin!==site.origin||callback.pathname!=="/auth/callback")invalid.push("SUPABASE_AUTH_CALLBACK_MISMATCH");}catch{invalid.push("SUPABASE_AUTH_CALLBACK_URL_INVALID");}}for(const name of Object.keys(process.env))if(name.startsWith("NEXT_PUBLIC_")&&/(SECRET|SERVICE|PASSWORD|TOKEN|PRIVATE|API_KEY)/.test(name)&&name!=="NEXT_PUBLIC_SUPABASE_ANON_KEY")invalid.push(`${name}_PUBLIC_SECRET_NAME`);const optional=["SUPABASE_AUTH_CALLBACK_URL","OPENDOTA_API_KEY","STEAM_WEB_API_KEY","ERROR_MONITORING_DSN","MONITORING_ENVIRONMENT","MONITORING_RELEASE","CRON_SECRET"].filter(x=>Boolean(process.env[x]));console.log(JSON.stringify({ok:!missing.length&&!invalid.length,mode:production?"production":"non-production",missing,invalid,optionalConfigured:optional}));if(missing.length||invalid.length)process.exitCode=1;
