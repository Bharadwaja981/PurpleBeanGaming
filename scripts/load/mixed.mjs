import{runHttp,target}from"./common.mjs";const base=target(),spectators=Number(process.env.CONCURRENCY??100),requests=Number(process.env.REQUESTS??1000),mutationUrl=process.env.AUCTION_MUTATION_URL,token=process.env.AUCTION_TEST_TOKEN,payload=process.env.AUCTION_TEST_PAYLOAD;if(!mutationUrl||!token||!payload)throw new Error("AUCTION_TEST_CONFIGURATION_REQUIRED");JSON.parse(payload);const publicRun=runHttp({base,paths:(process.env.PUBLIC_PATHS??"/,/leaderboards").split(","),concurrency:spectators,requests});const mutationRun=runHttp({base,paths:[mutationUrl],concurrency:Number(process.env.AUCTION_ACTORS??6),requests:Number(process.env.AUCTION_REQUESTS??60),method:"POST",body:payload,headers:{authorization:`Bearer ${token}`,"content-type":"application/json","x-draftgg-load-test":"true"}});const[publicResult,auctionResult]=await Promise.all([publicRun,mutationRun]);console.log(JSON.stringify({result:"COMPLETE",spectatorUsers:spectators,publicResult,auctionResult,invariants:"VERIFY_WITH_AUCTION_CONCURRENCY_SUITE"}));
