import { AppShell } from "@/components/layout/app-shell"; import { createClient } from "@/lib/supabase/server"; import { signOut } from "@/app/actions";
export default async function Dashboard(){const {data:{user}}=await (await createClient()).auth.getUser();return <AppShell><div className="flex items-start justify-between"><div><p className="page-kicker">Competition workspace</p><h1 className="page-title mt-2 font-bold">Tournament dashboard</h1><p className="muted mt-2">Signed in as {user?.email}</p></div><form action={signOut}><button className="button button-secondary">Sign out</button></form></div><div className="card mt-8 p-6"><p className="text-[var(--accent)]">Tournament ready</p><h2 className="mt-2 text-xl font-semibold">Purple Bean Test Cup</h2><p className="muted mt-2">Registration, auction and competition operations are available from the tournament navigation.</p></div></AppShell>}
