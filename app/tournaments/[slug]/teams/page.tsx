import Link from "next/link";import {Card} from "@/components/ui/card";import {PublicDraftNav} from "@/components/draft/public-nav";import {getPublicDraft} from "@/lib/tournament/public-draft";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const d=await getPublicDraft(slug);return <main className="mx-auto min-h-screen max-w-7xl space-y-6 p-4 py-10 md:p-8"><PublicDraftNav slug={slug}/><div><p className="text-sm uppercase tracking-[.2em] text-[var(--accent)]">Public team board</p><h1 className="mt-2 text-3xl font-bold">{d.tournament.name} Teams</h1></div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{d.teams.map(team=><Card className="p-5" key={team.id}><Link className="text-xl font-bold hover:text-[var(--accent)]" href={`/tournaments/${slug}/teams/${team.team_slug}`}>{team.short_tag} · {team.team_name}</Link><p className="muted mt-1">Captain {team.captain_name} · {team.captain_mmr} MMR</p><div className="mt-4 flex justify-between text-sm"><span>{team.current_team_mmr} total MMR</span><span>{team.credits_remaining} CR left</span></div><ul className="mt-4 space-y-2">{d.rosters.filter(p=>p.team_id===team.id).map(p=><li className="flex justify-between border-t border-[var(--line)] pt-2 text-sm" key={p.player_slug}><Link href={`/players/${p.player_slug}`}>{p.ign}</Link><span className="muted">{p.primary_role??"Flex"} · {p.purchase_price??0} CR</span></li>)}</ul></Card>)}</div></main>}
