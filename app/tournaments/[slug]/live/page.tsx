import type {Metadata} from "next";import {getPublicDraft} from "@/lib/tournament/public-draft";import {PublicLive} from "@/components/draft/public-live";import {PublicDraftNav} from "@/components/draft/public-nav";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const d=await getPublicDraft(slug);return{title:`${d.tournament.name} Live Draft`,description:`Watch the ${d.tournament.name} auction and team board live.`,openGraph:{title:`${d.tournament.name} Live Draft`,description:"Live Purple Bean Gaming auction spectator view."}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const data=await getPublicDraft(slug);return <main className="mx-auto min-h-screen max-w-7xl space-y-6 p-4 py-10 md:p-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[.2em] text-[var(--accent)]">Public spectator</p><h1 className="mt-2 text-3xl font-bold">{data.tournament.name}</h1><p className="muted mt-1">{data.tournament.spectator_delay_seconds?`${data.tournament.spectator_delay_seconds}s secure spectator delay`:`Committed live state`}</p></div><PublicDraftNav slug={slug}/></div><PublicLive slug={slug} initial={data}/></main>}
