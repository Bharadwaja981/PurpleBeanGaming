"use client"; import { useState } from "react"; import { createClient } from "@/lib/supabase/client";
export function SignInForm(){const [email,setEmail]=useState(""); const [message,setMessage]=useState(""); const magic=async()=>{const {error}=await createClient().auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/auth/callback`}});setMessage(error?.message??"Check your email for the sign-in link.")}; const google=()=>createClient().auth.signInWithOAuth({provider:"google",options:{redirectTo:`${location.origin}/auth/callback`}});return <div className="space-y-4"><button onClick={google} className="button button-secondary w-full">Continue with Google</button><label className="block text-sm">Email<input aria-label="Email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="mt-2"/></label><button onClick={magic} className="button button-primary w-full">Email magic link</button>{message&&<p aria-live="polite" className="muted text-sm">{message}</p>}</div>}
