# Leaderboards

The global leaderboard ranks career Competitive Rating deterministically by rating, match count, then public slug. Query parameters support role and minimum-match filtering, and the UI paginates the public projection to 100 rows per request. Season boards are isolated by season. Provisional players are displayed separately and labeled rather than mixed deceptively with established rankings. No private identity field is selected.
