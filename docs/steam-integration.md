# Steam integration

Steam supports identity and public profile evidence through `GetPlayerSummaries`. `STEAM_WEB_API_KEY` is server-only. Private or unavailable data is represented as private/unavailable and is never treated as misconduct. Account linking is user-declared unless an organizer or future official ownership flow explicitly verifies it.
