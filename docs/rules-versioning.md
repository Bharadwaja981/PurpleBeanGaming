# Tournament rules versioning

Published tournament rule versions are immutable. The tournament points to its active version while older content and acceptances remain intact. Each acceptance references the exact version, user, and tournament.

When `requires_reacceptance` is enabled, publishing a material update means participants without an acceptance for the new active version require a new acknowledgement. Publishing never rewrites previous rules or acceptance history.
